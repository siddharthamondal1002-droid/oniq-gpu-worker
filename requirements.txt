runpod==1.7.7
pillow==11.0.0
boto3==1.35.76
# torch 2.x does not depend on numpy; tensor.numpy() raises a bare
# RuntimeError without it — measured live 2026-08-26, job 4f15c663 (the
# first job to reach the GPU path, which no CPU test rig can execute).
numpy==2.1.3
# media-inference pins (2026-08-26): diffusers LTX pipelines + the T5
# tokenizer stack + mp4 encoding. Versions chosen against torch 2.5.1.
#
# BUMPED 0.33.1 -> 0.35.2 on 2026-08-29 for the model benchmark, and the
# reason is specific rather than "newer is better". MEASURED against the
# published wheels: 0.33.1 carries WanImageToVideoPipeline but its
# pipeline_wan_i2v.py has no `transformer_2` and no `boundary_ratio`, so it
# can drive Wan2.1 and CANNOT drive Wan2.2's two-expert mixture at all.
# 0.35.0 is the first release with both, and 0.35.2 is that minor's last
# patch — a smaller step than 0.36+, and every pipeline this worker needs
# (LTX image2video incl. FromSingleFileMixin for the 13B single-file
# checkpoint, Wan i2v, CogVideoX i2v) is present in all of them.
#
# The LTX PRODUCTION path moves with this bump, so it is not taken on
# faith: worker-ci imports every pipeline, and a real LTX 2B generation on
# the new image is run as a production canary before any benchmark job.
diffusers==0.35.2
# transformers >= 4.51 is REQUIRED by Qwen3: below it the config raises
# KeyError: 'qwen3' and the story model cannot load at all. The bump is
# proven against diffusers 0.33.1 / torch 2.5.1 by worker-ci's import
# test before anything is baked — LTX runs on those pins.
transformers==4.51.3
accelerate==1.2.1
sentencepiece==0.2.0
protobuf==5.29.3
imageio==2.36.1
imageio-ffmpeg==0.5.1
# audio_mux pins (2026-08-26): in-process TTS + mux, no subprocess.
# piper-phonemize ships manylinux_2_28 cp311 wheels (bookworm glibc 2.36
# accepts them); av bundles its own ffmpeg libs. All four proven to
# import and run a real synth+mux on python 3.11.15 before pinning.
piper-tts==1.2.0
piper-phonemize==1.1.0
onnxruntime==1.29.0
av==13.1.0

# 4-bit loading for the story model. The A5000 runs it ALONE (staged
# execution: story, unload, then LTX), so bf16 would also fit — 4-bit is
# for load time and headroom, not because the card is short.
bitsandbytes==0.45.0

# Model-benchmark runtime deps (2026-08-29). Named explicitly rather than
# left transitive: the probe downloads checkpoints at job time, and a
# dependency that arrives only as somebody else's transitive pin is one
# that can vanish under a resolver.
#
# huggingface_hub: diffusers 0.35.2 requires >=0.34.0, and modelprobe calls
#   snapshot_download directly. transformers 4.51.3 accepts <1.0.
# regex: `import regex as re` at the top of Wan's i2v pipeline — a hard
#   import, not an optional one. Without it Wan cannot be constructed.
# ftfy: Wan's text path checks is_ftfy_available() and cleans prompts with
#   it when present. Optional, and included so the benchmark measures Wan
#   as its reference implementation runs it rather than a degraded text
#   path.
huggingface_hub==0.34.6
regex==2026.7.19
ftfy==6.3.1
